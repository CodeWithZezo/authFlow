# ─── Development ──────────────────────────────────────────────────
# Vite proxy forwards /api → http://localhost:5000, so base URL is empty.
# This means all fetch("/api/...") calls go through the Vite dev proxy.
VITE_API_BASE_URL=https://authflow-api.codewithzezo.site
