# Copy this to .env.development or .env.production and fill in values.
# In development, leave VITE_API_BASE_URL empty — Vite proxy handles routing.
# In production, set it to your deployed backend URL (no trailing slash).

VITE_API_BASE_URL=
